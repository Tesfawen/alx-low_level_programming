#include "main.h"

/**
 * _abs - Computer the absolute value of integer.
 * @n: The integer to be computed
 *
 * Return: The absolute value of integer.
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
